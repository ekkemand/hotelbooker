@model IEnumerable<BLL.App.DTO.HotelConvenience>

@{
    ViewData["Title"] = "Hotel conveniences";
}

<h1>Hotel conveniences</h1>

<p>
    <a asp-action="Create">Add New</a>
</p>
<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Hotel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Convenience)
        </th>

        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Hotel!.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Convenience!.Name)
            </td>

            <td>
                @if (User.IsInRole("admin"))
                {
                    <div class="btn-group">
                        <a asp-action="Edit" class="btn btn-outline-info" asp-route-id="@item.Id">Edit</a>
                        <a asp-action="Delete" class="btn btn-outline-danger" asp-route-id="@item.Id">Delete</a>
                    </div>
                }
            </td>
        </tr>
    }
    </tbody>
</table>